# Install Playwright and its browsers in a Python virtual environment
# Usage: .\\.venv\\Scripts\\Activate.ps1 then run this script

pip install playwright
python -m playwright install

Write-Host "Playwright installed. Browsers downloaded."
